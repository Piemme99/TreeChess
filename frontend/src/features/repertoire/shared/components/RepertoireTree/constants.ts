/** Radius of each tree node circle/square */
export const NODE_RADIUS = 16;

/** Horizontal spacing between tree levels */
export const NODE_SPACING_X = 100;

/** Vertical spacing between sibling nodes */
export const NODE_SPACING_Y = 50;

/** Offset from left edge for root node */
export const ROOT_OFFSET_X = 60;

/** Offset from top edge for root node */
export const ROOT_OFFSET_Y = 40;

/** Default dimensions for initial render */
export const DEFAULT_WIDTH = 600;
export const DEFAULT_HEIGHT = 400;

/** Zoom limits */
export const MIN_ZOOM = 0.2;
export const MAX_ZOOM = 3;
